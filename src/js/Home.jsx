//import React, { useState } from "react";
//import '../styles/index.css';

//const Home = () => {
    //const [color, setColor] = useState("");

    //return (
        //<div className="semaforo">
            //<div
                //onClick={() => setColor("rojo")}
                //className={"luz rojo" + (color === "rojo" ? " activo" : "")}>
            //</div>
            //<div
                //onClick={() => setColor("amarillo")}
                //className={"luz amarillo" + (color === "amarillo" ? " activo" : "")}>
            //</div>
            //<div
                //onClick={() => setColor("verde")}
                //className={"luz verde" + (color === "verde" ? " activo" : "")}>
            //</div>
        //</div>
   // );
//};

import React from "react";

const Home = () => {
    return (
        <div>
            <h1>¡Hola, React funciona!</h1>
        </div>
    );
};

export default Home;
